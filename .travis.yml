language: android
android:
  components:
    - platform-tools
    - tools
    - build-tools-25.0.2
    - android-25
# Support library
    - extra-android-support
    - extra-android-m2repository

jdk:
  - oraclejdk8

before_script:
    - chmod +x gradlew

script:
   - chmod +x ./gradlew
   - ./gradlew assembleDebug assembleRelease
